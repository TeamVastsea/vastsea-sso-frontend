name: Lint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  checks: write
  contents: write

jobs:
  run-linters-frontend:
    name: Run linters frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 23
          cache: 'pnpm'
      - name: Install dependencies
        working-directory: packages/frontend
        run: pnpm i --frozen-lockfile --prefer-offline
      - name: Run linters
        uses: wearerequired/lint-action@v2
        with:
          eslint: true
          eslint_dir: packages/frontend
          auto_fix: true

  run-linters-backend-libs:
    name: Run linters backend libs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 23
          cache: 'pnpm'
      - name: Install dependencies
        working-directory: packages/backend
        run: pnpm prisma generate && pnpm i --frozen-lockfile --prefer-offline
      - name: Run linter
        uses: wearerequired/lint-action@v2
        with:
          eslint: true
          eslint_dir: packages/backend/libs
          auto_fix: true

  run-linters-backend-src:
    name: Run linters backend src
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 23
          cache: 'pnpm'
      - name: Install dependencies
        working-directory: packages/backend
        run: pnpm prisma generate && pnpm i --frozen-lockfile --prefer-offline
      - name: Run linter
        uses: wearerequired/lint-action@v2
        with:
          eslint: true
          eslint_dir: packages/backend/src
          auto_fix: true

  run-linters-backend-test:
    name: Run linters backend test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 23
          cache: 'pnpm'
      - name: Install dependencies
        working-directory: packages/backend/
        run: pnpm prisma generate && pnpm i --frozen-lockfile --prefer-offline
      - name: Run linter
        uses: wearerequired/lint-action@v2
        with:
          eslint: true
          eslint_dir: packages/backend/test
          auto_fix: true