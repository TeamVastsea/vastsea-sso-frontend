import { SuperSerializerInterceptor } from './super_serializer.interceptor';

describe('SuperSerializerInterceptor', () => {
  it('should be defined', () => {
    expect(new SuperSerializerInterceptor()).toBeDefined();
  });
});
